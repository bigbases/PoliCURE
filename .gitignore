__pycache__/
*.pyc
.DS_Store
*.tmp
.gitignore

# Full-text intermediates and secrets must never be committed.
*_scored.csv
.env

# Downloaded source corpora and converted full-text CSVs (never commit these).
Article-Bias-Prediction/
CheckThat/
Semeval2019t4/
original_dataset/

# Reassembled SemEval release (>100 MB). Commit the parts in dataset/semeval_parts/
# instead, and rebuild this file locally with merge_semeval.py.
dataset/semeval.csv
